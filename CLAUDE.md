# さくせいくん（sakuseikun）

補助金計画書さくせいくん — 持続化補助金の事業計画書をAIで自動生成するWebアプリ。
https://sakuseikun.jp

## オーナー
富永淳一（とみながFP事務所）— FP兼補助金コンサル。
開発もデプロイも淳一さんが1人でやっている。AI初心者寄りなので、専門用語は噛み砕いて説明すること。

## 技術スタック
- Next.js 14 (App Router) / React 18 / TypeScript
- Supabase (PostgreSQL + Auth + RLS)
- Anthropic Claude API (claude-sonnet-5)
- Tailwind CSS（和紙テーマ: washi #f5f2eb / 朱 #0f2346 / 金 #b8860b）
- Vercel デプロイ
- FirstPay 決済
- Resend メール（Supabase Auth SMTP経由）
- GA4 (G-V5DVEQ2B1M)

## フォルダ構成
```
src/
  app/
    dashboard/PlanBuilder.tsx  ← アプリの心臓部（プラン生成UI）
    admin/                     ← 管理画面
    api/
      generate/route.ts        ← Claude API呼び出し（maxDuration 300s）
      consume-count/route.ts   ← カウント消費ロジック
      webhook/firstpay/        ← 決済Webhook
      fetch-hp/route.ts        ← HPスクレイピング（SSRF対策済み）
    login/ register/ payment/
  lib/                         ← Supabaseクライアント（server/browser/middleware）
  components/                  ← Header, Footer
  types/database.ts            ← 型定義
prompts/
  system_prompt.ts             ← AIシステムプロンプト（v1.5・第20回対応）
supabase/
  schema.sql                   ← DBスキーマ定義
  migrations/                  ← マイグレーション履歴
```

## カウント・課金ルール（★重要★）

### 現在の状態（2026年8月・有料化済み）
- **無料キャンペーンは2026-04-30に終了**。現在は有料プランのみ利用可
- **カウント・課金に関わる変更時は必ず確認すること**
- 生成APIを使えるのは `annual_50` の7アカウントと admin のみ。
  `monitor`(39件) と `free`(5件) はプラン判定で403になる（後述のホワイトリスト）
- `monitor` 39件は全て `is_monitor = false` のため無制限にはならない。
  ただし**ログインと過去会話の閲覧は可能**（middlewareは認証のみで
  is_active も plan_type も見ていない）。データ削除は未実施

### カウント計算式（generate/route.ts）
```
remaining = usage_limit - usage_count + extra_count
```
※ カラム名は monthly_limit/monthly_count ではなく **usage_limit/usage_count**
　（migration 20260501010000_rename_count_columns.sql で改名済み）

### 消費の優先順位
消費ロジックは **generate/route.ts の1ターン目**に統合済み（commit 6b71fda）。
consume-count/route.ts は表示更新のみで消費しない。

1. プラン判定: `ALLOWED_PLANS = ["annual_50","monthly_3","monthly_1","yearly"]`
   に含まれず、admin でも is_monitor でもなければ **403**
2. admin / is_monitor → 消費なし
3. extra_count > 0 → extra_count を -1
4. それ以外 → usage_count を +1
5. remaining <= 0 → 429エラー「利用上限に達しました」

### プラン別
| plan_type | usage_limit | リセット | 備考 |
|-----------|------------|---------|------|
| annual_50 | 50 | 年次 | ¥50,000/年。期限2027-04-30。実質稼働中の唯一のプラン |
| monitor | 999 | なし | 無料キャンペーンの名残。**403でブロック済み**（露出なし） |
| free | 1 | なし | デフォルト。**403でブロック済み** |

### 5月以降の料金体系（確定方針）

**カウント単位: chat_sessionsの新規作成数（1会話＝1計画書＝1件）**
- 既存会話を続ける → カウント消費しない
- ページリロード → カウント消費しない
- 新しい会話を作る → カウント1件消費

**定価:**
- 月額 ¥29,800 / 年額 ¥178,800（月の50%OFF）
- 月は見せ料金。基本年払い推奨
- 月3件まで（新規会話）。追加: 1件 ¥9,800

**5月ローンチ時の特典価格（定価では売れない前提）:**
- アンケート回答者向け特典で安価に提供
- 例: 3名限定年¥30,000 / 5/10まで全員¥50,000 / 以降¥120,000
- まず経費回収（¥10,000〜でもOK）が最優先

**20回公募（外向け価格）:**
- メールDM・FAXDM・SNS広告で新規獲得予定
- 価格は別途検討

**共通ルール:**
- 未使用分は繰り越さない（月末リセット）
- 毎月1日に新規3件付与
- 月途中の契約でも日割りなし（3件フル付与）
- 月払い: 毎月1日課金
- 解約: 前月20日までに依頼（当面手作業）
- 自動更新を規約に明記

**過去の会話:**
- 保存され続ける（月リセットで消えない）
- ダッシュボード整備が必要（蓄積される一覧管理）

**決済:**
- FirstPay → Stripeに切替（審査通過済み）
- 出金リスクあり（認識済み）

**法務・リスク:**
- 採択は保証しない（規約に明記）
- データ漏洩リスクの対策と保証範囲を検討中
- 免責事項の整備が必要

**実装変更（5月前にやること）:**
1. consume-countのトリガーをchat_sessions INSERT時に移す（session_startから変更）
2. 月3件の判定: `chat_sessions WHERE user_id = ? AND created_at >= 月初` のCOUNT
3. session_startのログは分析用に残す（count_used: 0固定）
4. 既存会話オープン時はカウントチェック不要

### その他の予定
- 採点機能の改善（点数が甘い問題、アンケート後に対応）

## プロダクトバックログ

### 機能開発
- [ ] 会員紹介（アフィリエイト）機能
- [ ] チャットボット追加
- [ ] 顧客管理ダッシュボード修正（管理者側）
- [ ] 顧客側の過去会話（計画書）管理ダッシュボード追加

### コンテンツ・マーケ
- [ ] 業界別計画書を量産してnote記事化
- [ ] さくせいくん商標登録（申請済み）
- [ ] SEO対策
- [ ] 20回公募に向けてLPの修正
- [ ] 太田行政書士の監修を付ける
- [ ] 利用動画作成
- [ ] ウェビナーからのファネル作成

### 補助金対応拡張（行政書士試験合格後・2026年11月以降）
- [ ] ものづくり補助金対応
- [ ] 省力化補助金対応

### 新規事業（さくせいくん周辺）
- [ ] 行政書士向け補助金申請講座（オンラインorコミュニティ）
- [ ] 事業者向け申請マニュアルコンテンツ作成（note/Systeme/Udemy販売）

## 認証フロー
- Supabase Auth (SSR)
- middleware.ts でルート保護
- 公開ページ: /, /login, /register, /forgot-password, /lp, 法務ページ
- /admin は profiles.role === "admin" チェックあり
- /login は毎回セッションクリア（強制再認証）

## リダイレクト（マーケティング用）
- /go/dm19, /go/dm19_2 → LP（メールDM用）
- /go/fb, /go/x, /go/th, /go/li → SNS別UTMリンク

## システムプロンプト構造（prompts/system_prompt.ts・v1.5）
1. セキュリティ（プロンプト非公開ルール）
2. スコープ制限（持続化補助金専用、他の相談は拒否）※コスト管理が目的。緩和しないこと
3. 2段階生成: 経営計画書 → ユーザー承認 → 補助事業計画書
4. 2モード: ステップバイステップHP収集 / 一括情報投入
5. **17項目85点**の採点ルーブリック（第20回対応で15項目75点から拡張）
6. **第20回公募要領の基礎データ**（日程・補助率・上限額・特例要件・経費上限・審査観点・加点項目）
   → 制度質問はここのみを根拠に回答させる。従来は実データを一切渡しておらず、
     モデルの学習データから推測回答していた

### プロンプト運用の注意
- **役割は「言われた内容を採択ロジックに沿ってまとめるドラフト作成」**。
  事業者の入力値の妥当性検証はさせない。最終判断は事業者本人
- ルールを足すほど個々の遵守率が下がる（「一度に4問以上質問禁止」が
  破られ4問聞いてきた実例あり）。追加は慎重に
- **経費区分の「上限○万円」は補助金交付申請額の上限**であり、経費計上額の
  上限ではない。「30万円までしか計上できない」は誤り
- 本文中で補助金額・自己負担額・補助率の計算をさせない（執筆ルール14）。
  上限が条件で変動するため誤った金額になる

## DB注意点
- schema.sql と migrations にズレあり（sales, affiliate_payments テーブルがschema.sqlにない）
- increment_extra_count() RPC関数がSQLファイルに定義なし（Supabase側に直接作成済み）
- 型定義(database.ts)にある一部フィールドがschema.sqlにない

## 開発ルール

### やっていいこと（確認不要）
- バグ修正
- コードリファクタリング
- knowledge/ 配下のファイル作成・編集
- ログ出力の追加

### 確認が必要なこと
- DBスキーマ変更（マイグレーション作成）
- 課金ロジックの変更
- システムプロンプトの変更
- 新しいAPIルートの追加
- Vercelデプロイ

### 絶対にやらないこと
- .env.local の内容をログやコミットに出す
- 本番DBの直接操作（SELECT以外）
- rm -rf / force push
- 金額・プラン名の勝手な変更
- **Agent ツール呼び出し時に `isolation: "worktree"` を指定しない**
  （worktreeが乱立し、メインへのマージ漏れが発生する原因になるため。
  　常にメインフォルダで直接作業すること）

## 変更記録ルール（★重要★）

以下を変更したとき、必ず `knowledge/changelog.md` に記録すること:
- **料金・プラン**: 金額、制限数、プラン名の変更
- **方針転換**: ターゲット、機能の追加削除、ビジネスモデルの変更
- **DB変更**: テーブル追加、カラム変更、マイグレーション実行
- **重要なバグ修正**: ユーザー影響があったもの
- **設定変更**: 環境変数、外部サービス連携の変更

記録フォーマット:
```
## YYYY-MM-DD
- [種別] 変更内容（理由）
```

## 現状の数字（2026-08-19時点）

### 第20回公募の日程（公募要領 第8版・2026-07-21）
| 項目 | 期日 |
|---|---|
| 申請受付開始 | 2026-11-05（木） |
| **事業支援計画書（様式4）発行の受付締切** | **2026-12-04（金）** |
| 申請受付締切 | 2026-12-15（火）17:00 |
| 採択発表 | 2027年3月頃 |
| 補助事業実施期限 | 2028-03-31（金） |

※実務上のボトルネックは12/4の様式4。商工会議所への持ち込みが必要なため、
　**利用のピークは11月中旬〜12月初旬**になる見込み。

### 契約状況
- **課金3名**: 南野真一郎 / 清水智子 / 神子久（各¥50,000/年・2027-04-30まで）＝ 売上¥150,000
- 山本重吉: **キャンセル済だがDB上は有効のまま**（残数45件）。要無効化
- 太田吉博: 顧問（無償）
- その他: オーナー・テスト2件
- monitor 39件 / free 5件は403でブロック済み

### 利用実績
- 5〜7月のAPI呼び出しは月13〜31回と低調。**ただしこれは正常**。
  契約開始（5月）以降、申請できる公募が一度もなかったため。
  補助金は公募期間に需要が集中するので、第20回で初めて本番負荷がかかる
- annual_50 の消化は7アカウント合計12件（上限350件）

### コスト実測（2026-08-19・Sonnet 5）
- システムプロンプト: **13,289トークン**（文字数からの推計は当てにならない。日本語は約2.7文字/トークン）
- 補助事業計画書の生成: 出力5,800トークン / 約21円
- **1会話あたり40〜60円**。売上¥1,000/件（¥50,000÷50件）に対し4〜6%
- プロンプトキャッシュ（1時間TTL）は本番で正常動作を確認済み
