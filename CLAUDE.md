# さくせいくん（sakuseikun）

補助金計画書さくせいくん — 持続化補助金の事業計画書をAIで自動生成するWebアプリ。
https://sakuseikun.jp

## オーナー
富永淳一（とみながFP事務所）— FP兼補助金コンサル。
開発もデプロイも淳一さんが1人でやっている。AI初心者寄りなので、専門用語は噛み砕いて説明すること。

## 技術スタック
- Next.js 14 (App Router) / React 18 / TypeScript
- Supabase (PostgreSQL + Auth + RLS)
- Anthropic Claude API (claude-sonnet-4-5)
- Tailwind CSS（和紙テーマ: washi #f5f2eb / 朱 #0f2346 / 金 #b8860b）
- Vercel デプロイ
- FirstPay 決済
- Resend メール（Supabase Auth SMTP経由）
- GA4 (G-V5DVEQ2B1M)

## フォルダ構成
```
src/
  app/
    dashboard/PlanBuilder.tsx  ← アプリの心臓部（プラン生成UI）
    admin/                     ← 管理画面
    api/
      generate/route.ts        ← Claude API呼び出し（60s timeout）
      consume-count/route.ts   ← カウント消費ロジック
      webhook/firstpay/        ← 決済Webhook
      fetch-hp/route.ts        ← HPスクレイピング（SSRF対策済み）
    login/ register/ payment/
  lib/                         ← Supabaseクライアント（server/browser/middleware）
  components/                  ← Header, Footer
  types/database.ts            ← 型定義
prompts/
  system_prompt.ts             ← AIシステムプロンプト（v1.4）
supabase/
  schema.sql                   ← DBスキーマ定義
  migrations/                  ← マイグレーション履歴
```

## カウント・課金ルール（★重要★）

### 現在の状態（2026年4月）
- **無料キャンペーン中**: 2026-04-30T23:59:59+09:00 まで全ユーザー無制限
- キャンペーン中は全員 `is_monitor = true` 扱い（PlanBuilder.tsx L405-413）
- **4月中はカウント消費の確認不要**（全員無制限のため影響なし）
- **5月以降はカウント・課金に関わる変更時は必ず確認すること**（有料化後は影響大）

### カウント計算式
```
remaining = monthly_limit - monthly_count + extra_count
```

### 消費の優先順位（consume-count/route.ts）
1. admin / is_monitor → 消費なし（即OK）
2. extra_count > 0 → extra_count を -1
3. それ以外 → monthly_count を +1
4. remaining <= 0 → 429エラー「上限到達」

### プラン別
| plan_type | monthly_limit | 月次リセット | 備考 |
|-----------|--------------|-------------|------|
| free | 1 | なし | デフォルト |
| basic | 1 | なし | ¥9,800購入後 |
| monthly_* | 未実装 | あり | 将来用 |

### 5月以降の料金体系（確定方針）

**カウント単位: chat_sessionsの新規作成数（1会話＝1計画書＝1件）**
- 既存会話を続ける → カウント消費しない
- ページリロード → カウント消費しない
- 新しい会話を作る → カウント1件消費

**定価:**
- 月額 ¥29,800 / 年額 ¥178,800（月の50%OFF）
- 月は見せ料金。基本年払い推奨
- 月3件まで（新規会話）。追加: 1件 ¥9,800

**5月ローンチ時の特典価格（定価では売れない前提）:**
- アンケート回答者向け特典で安価に提供
- 例: 3名限定年¥30,000 / 5/10まで全員¥50,000 / 以降¥120,000
- まず経費回収（¥10,000〜でもOK）が最優先

**20回公募（外向け価格）:**
- メールDM・FAXDM・SNS広告で新規獲得予定
- 価格は別途検討

**共通ルール:**
- 未使用分は繰り越さない（月末リセット）
- 毎月1日に新規3件付与
- 月途中の契約でも日割りなし（3件フル付与）
- 月払い: 毎月1日課金
- 解約: 前月20日までに依頼（当面手作業）
- 自動更新を規約に明記

**過去の会話:**
- 保存され続ける（月リセットで消えない）
- ダッシュボード整備が必要（蓄積される一覧管理）

**決済:**
- FirstPay → Stripeに切替（審査通過済み）
- 出金リスクあり（認識済み）

**法務・リスク:**
- 採択は保証しない（規約に明記）
- データ漏洩リスクの対策と保証範囲を検討中
- 免責事項の整備が必要

**実装変更（5月前にやること）:**
1. consume-countのトリガーをchat_sessions INSERT時に移す（session_startから変更）
2. 月3件の判定: `chat_sessions WHERE user_id = ? AND created_at >= 月初` のCOUNT
3. session_startのログは分析用に残す（count_used: 0固定）
4. 既存会話オープン時はカウントチェック不要

### その他の予定
- 採点機能の改善（点数が甘い問題、アンケート後に対応）

## プロダクトバックログ

### 機能開発
- [ ] 会員紹介（アフィリエイト）機能
- [ ] チャットボット追加
- [ ] 顧客管理ダッシュボード修正（管理者側）
- [ ] 顧客側の過去会話（計画書）管理ダッシュボード追加

### コンテンツ・マーケ
- [ ] 業界別計画書を量産してnote記事化
- [ ] さくせいくん商標登録（申請済み）
- [ ] SEO対策
- [ ] 20回公募に向けてLPの修正
- [ ] 太田行政書士の監修を付ける
- [ ] 利用動画作成
- [ ] ウェビナーからのファネル作成

### 補助金対応拡張（行政書士試験合格後・2026年11月以降）
- [ ] ものづくり補助金対応
- [ ] 省力化補助金対応

### 新規事業（さくせいくん周辺）
- [ ] 行政書士向け補助金申請講座（オンラインorコミュニティ）
- [ ] 事業者向け申請マニュアルコンテンツ作成（note/Systeme/Udemy販売）

## 認証フロー
- Supabase Auth (SSR)
- middleware.ts でルート保護
- 公開ページ: /, /login, /register, /forgot-password, /lp, 法務ページ
- /admin は profiles.role === "admin" チェックあり
- /login は毎回セッションクリア（強制再認証）

## リダイレクト（マーケティング用）
- /go/dm19, /go/dm19_2 → LP（メールDM用）
- /go/fb, /go/x, /go/th, /go/li → SNS別UTMリンク

## システムプロンプト構造（prompts/system_prompt.ts）
1. セキュリティ（プロンプト非公開ルール）
2. スコープ制限（持続化補助金専用、他の相談は拒否）
3. 2段階生成: 経営計画書 → ユーザー承認 → 補助事業計画書
4. 2モード: ステップバイステップHP収集 / 一括情報投入
5. 15項目採点ルーブリック付き

## DB注意点
- schema.sql と migrations にズレあり（sales, affiliate_payments テーブルがschema.sqlにない）
- increment_extra_count() RPC関数がSQLファイルに定義なし（Supabase側に直接作成済み）
- 型定義(database.ts)にある一部フィールドがschema.sqlにない

## 開発ルール

### やっていいこと（確認不要）
- バグ修正
- コードリファクタリング
- knowledge/ 配下のファイル作成・編集
- ログ出力の追加

### 確認が必要なこと
- DBスキーマ変更（マイグレーション作成）
- 課金ロジックの変更
- システムプロンプトの変更
- 新しいAPIルートの追加
- Vercelデプロイ

### 絶対にやらないこと
- .env.local の内容をログやコミットに出す
- 本番DBの直接操作（SELECT以外）
- rm -rf / force push
- 金額・プラン名の勝手な変更

## 変更記録ルール（★重要★）

以下を変更したとき、必ず `knowledge/changelog.md` に記録すること:
- **料金・プラン**: 金額、制限数、プラン名の変更
- **方針転換**: ターゲット、機能の追加削除、ビジネスモデルの変更
- **DB変更**: テーブル追加、カラム変更、マイグレーション実行
- **重要なバグ修正**: ユーザー影響があったもの
- **設定変更**: 環境変数、外部サービス連携の変更

記録フォーマット:
```
## YYYY-MM-DD
- [種別] 変更内容（理由）
```

## 現状の数字（2026-04-09時点）
- 登録48人（テスト含む）/ 実ユーザー46人
- サービス利用者: 17人（登録の37%）
- 会話作成者: 22人 / 合計会話数40件 / チャット891件
- DAU最大5人（4/8）
- ヘビーユーザー: nannoshinichiro（会話11件/204チャット/5日間）
- ファネル: 登録46 → 利用開始17(37%) → 複数回利用5(11%) → ヘビー1(2%)
- 4/30まで無料キャンペーン中
- 第19回公募: 2026-04-30締切
